# 校园先到先得报名与智能排班系统（Windows 桌面版）架构设计

## 1. 目标与约束
**目标**：提供「报名-锁额-调剂-排班-导出」闭环的校园场景桌面应用，保证先到先得公平性、调剂与排班可控、权限边界清晰。  
**技术约束**：Python；Windows 桌面应用；**无浏览器网页 UI**（前端使用 Qt Widgets）。  
**质量要求**：分层清晰、可维护、可测试、符合开发规范。

## 2. 总体架构
采用**单机本地架构 + 分层设计**，以 PySide6（Qt Widgets）为前端，业务逻辑与数据访问解耦，SQLite 本地持久化。  

```
Presentation(UI, Qt Widgets)
    ↓
Application Services (用例编排/事务边界)
    ↓
Domain Model (实体/值对象/规则/策略)
    ↓
Infrastructure (SQLite/导出/日志/配置)
```

### 2.1 分层职责
| 层级 | 职责 | 关键点 |
| --- | --- | --- |
| UI 层 | 界面与交互、表单校验、状态展示 | 只做展示与输入，不包含业务规则 |
| 应用服务层 | 用例编排、事务、权限校验入口 | 调用领域对象完成操作 |
| 领域层 | 实体、值对象、领域服务、规则 | 业务规则集中，易测 |
| 基础设施层 | SQLite、导出、通知、日志、配置 | 可替换与隔离第三方依赖 |

## 3. 核心模块划分
1. **身份与权限模块**  
   - RBAC 模型，角色：超级管理员 / 组织者 / 普通用户  
   - 登录会话管理（单例 Session）、用户工厂创建  

2. **管理端与客户端分离**  
   - 管理端：活动管理、排班执行、用户管理、数据导出  
   - 客户端：报名、查看排班结果、个人信息  

3. **活动与规则模块**  
   - 活动实体、时段实体、规则配置  
   - 报名显示方式：**实时显示** / **非实时显示（盲报）**  
   - 排班方式：**志愿优先** / **先到先得** / **抽签随机**  
   - 活动状态机（状态模式：未开始/报名中/结束/归档）  

4. **报名与小组模块**  
   - 个人报名、组报名、名额锁定与释放  
   - 观察者模式通知名额变化（UI/通知）  

5. **调剂与排班模块**  
   - 多策略：志愿优先 / 先到先得 / 抽签  
   - 结果生成与手动调整  

6. **数据导出与统计模块**  
   - Excel 导出（pandas/openpyxl）  
   - 基础统计与图表（Qt Charts 或 Matplotlib）  

## 4. 关键设计与模式
| 场景 | 设计 | 说明 |
| --- | --- | --- |
| 角色创建 | 工厂模式 | 根据角色类型创建用户对象 |
| 登录会话 | 单例模式 | 统一管理当前登录用户与权限 |
| 活动生命周期 | 状态模式 | 不同状态下限制操作 |
| 名额变化通知 | 观察者模式 | UI 与通知模块订阅名额变化 |
| 排班算法 | 策略/领域服务 | 可替换算法实现 |

## 5. 领域模型（简化）
**核心实体**：User、Role、Activity(含 signup_mode/allocation_mode)、TimeSlot、Registration、Group、Adjustment、ScheduleResult  

### 5.1 数据表建议（SQLite）
| 表 | 关键字段 |
| --- | --- |
| users | id, name, role, password_hash, status |
| activities | id, name, status, owner_id, signup_start, signup_end, signup_mode, allocation_mode |
| slots | id, activity_id, start_time, end_time, capacity |
| registrations | id, user_id, activity_id, slot_id, priority, status, created_at |
| groups | id, leader_id, invite_code |
| group_members | group_id, user_id |
| adjustments | id, activity_id, user_id, from_slot, to_slot |
| schedule_results | id, activity_id, user_id, slot_id |

## 6. 关键用例流程（摘要）
1. **先到先得报名**  
   - UI → ApplicationService → RegistrationDomain  
   - SQLite 事务锁定名额：检查余量 → 写入报名 → 更新余量  

2. **调剂与排班**  
   - 报名截止后触发：按报名时间排序 → 依志愿分配剩余名额  
   - 生成排班结果 → 可手动调整 → 结果通知  

3. **权限控制**  
   - 每个应用服务入口校验角色与资源归属  
   - 组织者仅可操作自有活动  

## 7. 并发与事务策略
**场景**：多用户快速报名导致超报  
**策略**：  
1. 关键操作使用 SQLite 事务 (`BEGIN IMMEDIATE`)  
2. 单时段名额以原子更新语句减少竞争  
3. 失败时明确返回占用或超时结果  

## 8. 安全与规范
- 密码加密：`bcrypt`/`passlib`  
- 输入校验：UI 层 + 应用层双重校验  
- 日志：结构化日志（按模块与级别）  
- 配置：`config.json`/`ini`，本地可覆写  

## 9. 目录结构建议
```
app/
  ui/                # Qt 界面与控件
  application/       # 用例服务
  domain/            # 领域模型与规则
  infrastructure/    # SQLite/导出/日志/通知
  resources/         # 图标与静态资源
  tests/             # 单元/集成测试
```

## 10. 可扩展方向（非必须）
- **局域网协作模式**：保留接口层，可切换为局域网服务，但 UI 仍为桌面端  
- **算法策略扩展**：以策略模式支持更复杂排班算法  

## 11. 技术选型确认
- **UI**：PySide6（Qt Widgets）  
- **存储**：SQLite  
- **导出**：pandas/openpyxl  
- **图表**：Qt Charts / Matplotlib  

---
以上架构满足课程要求的分层设计与经典设计模式，同时保持单机可落地、易维护、可扩展。  
