# 🏗️ 校园先到先得报名与智能排班系统 - 架构设计文档

> **版本**: v1.0  
> **更新日期**: 2026-05-29  
> **文档状态**: 正式版

---

## 📋 目录

- [1. 架构概述](#1-架构概述)
- [2. 系统架构](#2-系统架构)
- [3. 分层设计](#3-分层设计)
- [4. 核心业务模块](#4-核心业务模块)
- [5. 关键业务规则](#5-关键业务规则)
- [6. 设计模式应用](#6-设计模式应用)
- [7. 部署架构](#7-部署架构)
- [8. 维护建议](#8-维护建议)

---

## 1. 架构概述

### 1.1 架构定位

当前项目**不是纯粹的前后端分离项目**，而是采用**桌面客户端 + 可选后端服务 + 本地数据库**的混合架构：

```
┌─────────────────────────────────────────────────────────────────┐
│                     当前架构形态                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│    ┌──────────────┐         ┌──────────────┐                   │
│    │  本地模式     │         │  远程模式     │                   │
│    │              │         │              │                   │
│    │  PySide6     │         │  PySide6     │                   │
│    │     │        │         │     │        │                   │
│    │     ▼        │         │     ▼        │                   │
│    │  SQLite      │         │  FastAPI     │                   │
│    │              │         │     │        │                   │
│    │              │         │     ▼        │                   │
│    │              │         │  SQLite      │                   │
│    └──────────────┘         └──────────────┘                   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 1.2 架构优势

| 优势 | 说明 |
|------|------|
| **灵活性** | 支持单机和多用户两种部署模式 |
| **可扩展性** | 后续可升级为标准三段式架构（前端、后端、数据库） |
| **低耦合** | 数据访问层抽象，本地/远程可无缝切换 |
| **易维护** | 分层清晰，各层职责明确 |

---

## 2. 系统架构

### 2.1 整体架构图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                              用户界面层                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │   登录界面   │  │   活动管理   │  │   报名界面   │  │   排班界面   │ │
│  │  LoginDialog │  │   Activity   │  │ Registration │  │  Scheduling  │ │
│  └──────────────┘  └──────────────┘  └──────────────┘  └──────────────┘ │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                             应用服务层                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │  UserService  │  │ActivityService│ │RegistrationSvc│ │SchedulingSvc │ │
│  │   用户服务   │  │   活动服务   │  │   报名服务   │  │   排班服务   │ │
│  └──────────────┘  └──────────────┘  └──────────────┘  └──────────────┘ │
│                                    │                                    │
│                    ┌───────────────┼───────────────┐                   │
│                    ▼               ▼               ▼                   │
│            ┌──────────────┐ ┌──────────────┐ ┌──────────────┐         │
│            │LocalService  │ │RemoteService │ │  Service     │         │
│            │  本地服务    │ │  远程服务    │ │  Factory     │         │
│            └──────────────┘ └──────────────┘ └──────────────┘         │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                              领域层                                     │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │    User      │  │   Activity   │  │ Registration │  │   Slot       │ │
│  │   用户模型   │  │   活动模型   │  │   报名模型   │  │   时段模型   │ │
│  └──────────────┘  └──────────────┘  └──────────────┘  └──────────────┘ │
│  ┌──────────────┐  ┌──────────────┐                                     │
│  │  Scheduler   │  │   Enums      │                                     │
│  │   排班算法   │  │   枚举定义   │                                     │
│  └──────────────┘  └──────────────┘                                     │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                           基础设施层                                     │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │   Database   │  │  API Server  │  │   Exporter   │  │    Auth      │ │
│  │   SQLite     │  │   FastAPI    │  │  Excel导出   │  │   认证模块   │ │
│  └──────────────┘  └──────────────┘  └──────────────┘  └──────────────┘ │
│  ┌──────────────┐  ┌──────────────┐                                     │
│  │  ApiClient   │  │    Config    │                                     │
│  │  HTTP客户端  │  │   配置管理   │                                     │
│  └──────────────┘  └──────────────┘                                     │
└─────────────────────────────────────────────────────────────────────────┘
```

### 2.2 运行模式详解

#### 本地模式

```
┌─────────────┐      ┌─────────────┐      ┌─────────────┐
│   PySide6   │ ───► │ LocalService│ ───► │   SQLite    │
│   客户端    │      │  本地服务   │      │   数据库    │
└─────────────┘      └─────────────┘      └─────────────┘
```

**特点:**
- 客户端直接访问 SQLite 数据库
- 无需网络通信，启动即可使用
- 适合单机开发、测试、演示

#### 远程模式

```
┌─────────────┐      ┌─────────────┐      ┌─────────────┐      ┌─────────────┐
│   PySide6   │ ───► │RemoteService│ ───► │  ApiClient  │ ───► │   FastAPI   │
│   客户端    │      │  远程服务   │      │  HTTP客户端 │      │   服务端    │
└─────────────┘      └─────────────┘      └─────────────┘      └──────┬──────┘
                                                                      │
                                                                      ▼
                                                               ┌─────────────┐
                                                               │   SQLite    │
                                                               │   数据库    │
                                                               └─────────────┘
```

**特点:**
- 客户端通过 HTTP 调用 FastAPI 后端
- 支持多用户并发访问
- 适合团队协作、生产环境

---

## 3. 分层设计

### 3.1 分层职责

| 层级 | 职责 | 代码位置 | 核心类 |
|------|------|---------|--------|
| **UI 层** | 用户界面、交互逻辑、状态展示 | `app/ui/` | MainWindow, LoginDialog, ActivityWidget |
| **应用层** | 用例编排、权限判断、调用仓储和领域服务 | `app/application/` | UserService, ActivityService, RegistrationService |
| **领域层** | 实体定义、业务规则、排班算法 | `app/domain/` | User, Activity, Registration, Scheduler |
| **基础设施层** | 数据存储、网络通信、外部集成 | `app/infrastructure/` | Database, ApiClient, Exporter, Auth |

### 3.2 依赖关系

```
┌─────────────────────────────────────────────────────────┐
│                      UI 层                               │
│                         │                               │
│                         ▼                               │
│                    应用服务层                             │
│                         │                               │
│         ┌───────────────┼───────────────┐               │
│         ▼               ▼               ▼               │
│    ┌─────────┐    ┌─────────┐    ┌─────────┐           │
│    │ 本地服务 │    │ 远程服务 │    │ 服务工厂 │           │
│    └────┬────┘    └────┬────┘    └─────────┘           │
│         │              │                               │
│         ▼              ▼                               │
│    ┌─────────┐    ┌─────────┐                          │
│    │  SQLite │    │FastAPI  │                          │
│    └─────────┘    └─────────┘                          │
│                                                         │
│                    基础设施层                             │
└─────────────────────────────────────────────────────────┘
```

**关键原则:**
- ✅ 上层依赖下层，下层不依赖上层
- ✅ 同层之间通过接口通信，不直接依赖实现
- ✅ 领域层不依赖任何外部框架

### 3.3 数据流转

```
用户操作 → UI 事件 → 应用服务 → 领域逻辑 → 基础设施 → 返回结果 → UI 更新
```

**示例: 用户报名流程**

1. 用户在 UI 点击「报名」按钮
2. UI 层调用 `RegistrationService.submit_registration()`
3. 应用层验证用户权限和报名规则
4. 应用层调用领域层 `Registration` 模型创建报名记录
5. 应用层调用基础设施层 `Database` 保存数据
6. 应用层调用基础设施层 `Database` 更新时段名额
7. 返回报名结果给 UI 层
8. UI 层更新界面显示

---

## 4. 核心业务模块

### 4.1 身份认证与 RBAC

**角色定义:**

| 角色 | 权限范围 | 典型操作 |
|------|---------|---------|
| **超级管理员** | 全局权限 | 管理所有用户、管理所有活动、系统配置 |
| **活动组织者** | 自有活动权限 | 创建活动、管理报名、执行排班、导出数据 |
| **普通用户** | 个人权限 | 报名活动、查看结果、查看排班 |

**认证流程:**

```
┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│  登录    │ ──► │ 验证凭证 │ ──► │ 生成JWT  │ ──► │ 返回Token│
└──────────┘     └──────────┘     └──────────┘     └──────────┘
                                                        │
                                                        ▼
┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│  响应    │ ◄── │ 业务逻辑 │ ◄── │ 验证权限 │ ◄── │ 解析Token│
└──────────┘     └──────────┘     └──────────┘     └──────────┘
```

### 4.2 活动管理

**活动生命周期:**

```
┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│  草稿    │ ──► │  报名中  │ ──► │ 报名结束 │ ──► │  已归档  │
└──────────┘     └──────────┘     └──────────┘     └──────────┘
     │                │                │
     │                │                │
     ▼                ▼                ▼
  可编辑           可报名           可排班
```

**状态转换规则:**
- 草稿 → 报名中: 组织者发布活动
- 报名中 → 报名结束: 到达报名截止时间 或 组织者手动结束
- 报名结束 → 已归档: 排班完成后归档

### 4.3 报名管理

**报名流程:**

```
┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│ 选择时段 │ ──► │ 检查名额 │ ──► │ 锁定名额 │ ──► │ 保存记录 │
└──────────┘     └──────────┘     └──────────┘     └──────────┘
                      │
                      ▼
                 ┌──────────┐
                 │ 名额不足 │
                 │ 返回错误 │
                 └──────────┘
```

**并发控制:**
- 使用数据库事务保证名额操作的原子性
- 使用乐观锁（版本号）防止并发冲突
- 超时未确认自动释放名额

### 4.4 排班管理

**排班算法:**

```python
def greedy_schedule(registrations, slots):
    """
    贪心排班算法
    
    规则:
    1. 按报名时间排序（先到先得）
    2. 按志愿优先级匹配（1志愿优先）
    3. 名额不足时自动调剂到下一志愿
    """
    results = []
    
    # 按报名时间排序
    sorted_regs = sorted(registrations, key=lambda r: r.created_at)
    
    for reg in sorted_regs:
        # 尝试按志愿优先级匹配
        for priority in [1, 2, 3]:
            slot = get_slot_by_priority(reg, priority)
            if slot and slot.has_capacity():
                slot.allocate(reg.user)
                results.append(ScheduleResult(reg.user, slot))
                break
        else:
            # 所有志愿都无名额，进入调剂队列
            add_to_waiting_list(reg)
    
    return results
```

---

## 5. 关键业务规则

### 5.1 报名规则

| 规则 | 说明 | 实现方式 |
|------|------|---------|
| 报名优先级最小值为 1 | 必须填写至少一个志愿 | 前端校验 + 后端校验 |
| 活动截止时间必须晚于开始时间 | 时间逻辑校验 | 前端校验 + 后端校验 |
| 实时报名模式下，名额不足时直接拒绝 | 防止超报 | 数据库事务 + 乐观锁 |
| 排班时同一用户只会被分配一个结果 | 防止重复分配 | 排班算法约束 |

### 5.2 权限规则

| 规则 | 说明 | 实现方式 |
|------|------|---------|
| 只有超级管理员和组织者可以创建活动 | 权限控制 | RBAC 模型 |
| 只有超级管理员或活动所有者可以新增时段 | 权限控制 | RBAC 模型 |
| 普通用户只能查看自己的报名和排班结果 | 数据隔离 | 查询条件过滤 |

### 5.3 数据一致性规则

| 规则 | 说明 | 实现方式 |
|------|------|---------|
| 名额操作必须原子性 | 防止并发冲突 | 数据库事务 |
| 报名取消后名额自动释放 | 保证名额可用 | 触发器/应用层逻辑 |
| 排班结果与报名记录一致 | 数据完整性 | 外键约束 |

---

## 6. 设计模式应用

### 6.1 模式总览

| 模式 | 应用场景 | 实现位置 |
|------|---------|---------|
| **工厂模式** | 创建不同角色的用户对象 | `UserService.create_user()` |
| **单例模式** | 管理全局登录状态 | `AuthManager` |
| **状态模式** | 管理活动生命周期 | `Activity.status` |
| **观察者模式** | 名额变化通知 | `Slot.on_capacity_change()` |
| **策略模式** | 不同排班算法切换 | `Scheduler` |
| **仓储模式** | 数据访问抽象 | `Repository` 接口 |

### 6.2 工厂模式示例

```python
class UserFactory:
    """用户工厂 - 根据角色创建不同的用户对象"""
    
    @staticmethod
    def create_user(username: str, password: str, role: UserRole) -> User:
        if role == UserRole.SUPER_ADMIN:
            return SuperAdmin(username, password)
        elif role == UserRole.ORGANIZER:
            return Organizer(username, password)
        elif role == UserRole.USER:
            return RegularUser(username, password)
        else:
            raise ValueError(f"Unknown role: {role}")
```

### 6.3 状态模式示例

```python
class ActivityStatus(Enum):
    DRAFT = "draft"
    REGISTRATION_OPEN = "registration_open"
    REGISTRATION_CLOSED = "registration_closed"
    ARCHIVED = "archived"

class Activity:
    def __init__(self):
        self._status = ActivityStatus.DRAFT
    
    def publish(self):
        """发布活动"""
        if self._status != ActivityStatus.DRAFT:
            raise InvalidStateTransition("Only draft activities can be published")
        self._status = ActivityStatus.REGISTRATION_OPEN
    
    def close_registration(self):
        """关闭报名"""
        if self._status != ActivityStatus.REGISTRATION_OPEN:
            raise InvalidStateTransition("Only open activities can be closed")
        self._status = ActivityStatus.REGISTRATION_CLOSED
```

### 6.4 仓储模式示例

```python
from abc import ABC, abstractmethod

class UserRepository(ABC):
    """用户仓储接口"""
    
    @abstractmethod
    def get_by_id(self, user_id: str) -> Optional[User]:
        pass
    
    @abstractmethod
    def save(self, user: User) -> None:
        pass
    
    @abstractmethod
    def delete(self, user_id: str) -> None:
        pass

class SQLiteUserRepository(UserRepository):
    """SQLite 用户仓储实现"""
    
    def get_by_id(self, user_id: str) -> Optional[User]:
        # SQLite 实现
        pass

class RemoteUserRepository(UserRepository):
    """远程用户仓储实现"""
    
    def get_by_id(self, user_id: str) -> Optional[User]:
        # HTTP API 实现
        pass
```

---

## 7. 部署架构

### 7.1 本地模式部署

```
┌─────────────────────────────────────────┐
│              单机环境                     │
│  ┌─────────────┐    ┌─────────────┐     │
│  │   PySide6   │    │   SQLite    │     │
│  │   客户端    │◄──►│   数据库    │     │
│  └─────────────┘    └─────────────┘     │
└─────────────────────────────────────────┘
```

**部署步骤:**
1. 安装 Python 3.9+
2. 安装依赖: `pip install -r requirements.txt`
3. 运行: `python -m app.main`

### 7.2 远程模式部署

```
┌─────────────────────────────────────────────────────────┐
│                    服务器环境                             │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐ │
│  │   FastAPI   │◄──►│   SQLite    │    │   Nginx     │ │
│  │   服务端    │    │   数据库    │    │  (可选)     │ │
│  └─────────────┘    └─────────────┘    └─────────────┘ │
└─────────────────────────────────────────────────────────┘
                           │
                           │ HTTP/HTTPS
                           ▼
┌─────────────────────────────────────────────────────────┐
│                    客户端环境                             │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐ │
│  │   PySide6   │◄──►│   客户端A   │    │   客户端B   │ │
│  │   客户端    │    │             │    │             │ │
│  └─────────────┘    └─────────────┘    └─────────────┘ │
└─────────────────────────────────────────────────────────┘
```

**部署步骤:**
1. 服务器安装 Python 3.9+
2. 安装依赖: `pip install -r requirements.txt`
3. 启动后端: `uvicorn app.api_server:app --host 0.0.0.0 --port 8000`
4. 客户端配置服务器地址
5. 运行客户端: `python -m app.main --remote`

---

## 8. 维护建议

### 8.1 代码规范

| 规范 | 说明 |
|------|------|
| **业务逻辑位置** | 保留应用层与领域层，不要把业务逻辑写进 UI 或 API 路由 |
| **返回值规范** | 接口返回值统一使用字典或 DTO，不要直接暴露数据库游标对象 |
| **接口一致性** | 远程模式下补齐 metrics 与 schedules 查询接口，避免客户端仓储与后端不一致 |
| **数据库迁移** | 后续如果升级数据库，先保持仓储层接口不变，再替换存储实现 |

### 8.2 扩展建议

| 扩展方向 | 建议 |
|---------|------|
| **数据库升级** | 从 SQLite 升级到 PostgreSQL，保持仓储层接口不变 |
| **前端扩展** | 可增加 Web 前端、移动端，复用后端 API |
| **功能扩展** | 可增加消息通知、邮件提醒、数据导出等功能 |
| **性能优化** | 可增加缓存层、异步处理、数据库索引优化 |

### 8.3 测试策略

| 测试类型 | 覆盖范围 | 工具 |
|---------|---------|------|
| **单元测试** | 领域层、应用层逻辑 | pytest |
| **集成测试** | 数据库、API 接口 | pytest + httpx |
| **UI 测试** | 界面交互 | pytest-qt |
| **端到端测试** | 完整业务流程 | 手动测试 + 自动化脚本 |

---

## 📚 相关文档

- [项目设计文档](生活项目：校园先到先得报名与智能排班系统.md) - 项目背景、功能模块、技术选型
- [数据库设计与 API 文档](docs/数据库设计与API文档.md) - 数据库表结构和接口设计
- [后端接口文档](docs/后端接口文档.md) - FastAPI 接口详细说明

---

<p align="center">
  <strong>📝 文档维护者: LumenAurora</strong>
</p>
