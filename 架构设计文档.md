# 校园先到先得报名与智能排班系统架构设计

## 1. 结论
当前项目**不是纯粹的前后端分离项目**。它已经具备一个可切换的数据访问边界，但主形态仍然是桌面端应用：

- 本地模式：PySide6 桌面客户端直接访问 SQLite
- 远程模式：PySide6 桌面客户端通过 HTTP 调用 FastAPI 后端

因此，当前更准确的描述是：**桌面客户端 + 可选后端服务 + 本地数据库**。如果后续需要长期维护，建议进一步收敛成标准的三段式结构：前端、后端、数据库。

## 2. 当前架构
```
桌面客户端（app/ui）
    ↓
应用服务层（app/application）
    ↓
领域层（app/domain）
    ↓
基础设施层（SQLite / HTTP API / 导出 / 配置）
```

### 2.1 当前运行模式
1. 本地模式
   - `app/main.py` 直接初始化 SQLite
   - UI 调用本地仓储与服务
2. 远程模式
   - `app/main.py` 根据配置切换到 `ApiClient`
   - UI 通过 `Remote*Service` 和 `Remote*Repository` 访问后端

### 2.2 代码分层职责
| 层级 | 作用 | 代码位置 |
| --- | --- | --- |
| UI 层 | 登录、列表、表单、导航、状态展示 | [app/ui](app/ui) |
| 应用层 | 用例编排、权限判断、调用仓储和领域服务 | [app/application](app/application) |
| 领域层 | 实体、枚举、排班算法、异常 | [app/domain](app/domain) |
| 基础设施层 | SQLite、HTTP 客户端、密码、导出、远程仓储 | [app/infrastructure](app/infrastructure) |

## 3. 前后端拆分建议
如果要拆成标准前后端分离，建议保持以下边界：

- 前端：PySide6 客户端，只负责界面、状态流转和 API 调用
- 后端：FastAPI 服务，只负责认证、业务规则、数据读写和调度
- 数据库：SQLite 可作为开发库，生产建议升级为 PostgreSQL

这种拆法的好处是：

- 客户端与服务端可独立部署
- 数据库可独立迁移和备份
- 接口契约固定后，后续可以增加 Web、移动端或管理后台

## 4. 核心业务模块
1. 身份认证与 RBAC
   - 角色：超级管理员、组织者、普通用户
   - 登录后以 token 方式访问服务端
2. 活动管理
   - 创建活动、配置报名时间、报名方式、排班方式
3. 时段管理
   - 为活动维护多个时段及容量
4. 报名管理
   - 用户提交报名记录，实时模式下会锁定名额
5. 排班管理
   - 按优先级、先到先得或抽签规则生成排班结果
6. 数据统计与导出
   - 统计活动、时段、报名、排班结果数量
   - 导出 Excel 报表

## 5. 关键业务规则
- 报名优先级最小值为 1
- 活动截止时间必须晚于开始时间
- 只有超级管理员和组织者可以创建活动
- 只有超级管理员或活动所有者可以新增时段
- 实时报名模式下，时段名额不足时直接拒绝报名
- 排班时同一用户只会被分配一个结果

## 6. 现阶段的维护建议
- 保留应用层与领域层，不要把业务逻辑写进 UI 或 API 路由
- 接口返回值统一使用字典或 DTO，不要直接暴露数据库游标对象
- 后续如果升级数据库，先保持仓储层接口不变，再替换存储实现

## 7. 文档索引
- [数据库设计与 API 文档](docs/数据库设计与API文档.md)
- [后端接口文档](docs/后端接口文档.md)