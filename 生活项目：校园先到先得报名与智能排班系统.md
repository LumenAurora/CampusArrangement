# 生活项目：校园先到先得报名与智能排班系统
## 项目功能设计文档
### 一、项目背景与核心目标
#### 背景
校园场景中，志愿服务报名、课堂pre名额预约、活动时段排班等需求高频出现，现有工具（问卷星、Excel、微信群）存在三大核心痛点：1）无法实现精准的先到先得名额锁定，易出现超报、抢票混乱；2）不支持多志愿调剂与自动排班，组织者需手动统计协调，耗时耗力且易出错；3）无分层权限管理，无法区分管理员、组织者、参与者的操作边界。本项目为Qt原生桌面端工具，完全贴合校园真实场景，打造「报名-锁额-调剂-排班-导出」全流程闭环。
#### 核心目标
1.  解决校园多时段活动先到先得报名、小组报名、多志愿调剂的核心痛点，替代手动Excel统计；
2.  实现三级权限精细化管理，适配超级管理员、活动组织者、普通参与者的不同需求；
3.  完整体现课程要求的分层架构、面向对象设计思想与经典设计模式，功能边界清晰、开发成本可控。

---

### 二、核心功能模块

经典平台类软件设计，分级权限管理，客户端与服务端分离，前后端分离，前端-PyQt，后端-Python flask，数据库-SQLite。

| 模块名称                      | 核心功能描述                                                 | 实现逻辑与成本控制                                           |
| ----------------------------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| 1. 三级权限与用户管理模块     | 1. 角色划分：超级管理员（全局权限，管理所有用户与活动）、活动组织者（创建/管理自有活动、配置规则、导出数据）、普通用户（报名活动、结组、查看排班结果）；<br>2. 用户基础功能：注册/登录、个人信息维护、报名历史查询；<br>3. 权限精细化控制：不同角色对应不同操作边界，越权操作拦截 | 基于RBAC（基于角色的访问控制）模型设计，用**工厂模式**创建不同角色的用户对象，用**单例模式**管理全局登录状态与权限校验；基于SQLite实现用户与权限数据存储，无第三方服务依赖 |
| 2. 活动创建与规则配置模块     | 1. 活动基础配置：活动名称、时间、报名起止时间、活动详情；<br>2. 时段与名额配置：支持多时段独立设置（如志愿服务8:00-10:00/10:00-12:00）、单时段名额上限、单人最大报名时段数；<br>3. 报名规则配置：开启/关闭先到先得锁额、是否支持小组报名、是否允许多志愿填报（最多3个志愿）、名额锁定超时时间；<br>4. 活动状态管理：支持活动发布、暂停、结束、归档全流程控制 | 用**状态模式**管理活动全生命周期（未开始、报名中、报名结束、已归档）；所有规则配置均为可视化勾选/填写，无需代码修改；基于SQLite实现活动配置数据存储，开发成本极低 |
| 3. 先到先得报名与小组管理模块 | 1. 个人报名：用户可查看活动时段剩余名额，按志愿优先级填报时段，提交后即时锁定名额（超时未确认自动释放），实时更新剩余名额；<br>2. 小组报名：支持组长创建小组、成员通过邀请码加入，小组整体填报志愿、抢占名额，锁定时段后全员同步报名结果；<br>3. 报名管理：支持取消报名、自动释放名额、报名状态实时同步 | 用**观察者模式**实现名额变化的实时更新与通知；用SQLite事务机制保证名额操作的原子性，彻底解决超报、重复锁额问题；无需实时网络服务，单机即可实现精准的先到先得逻辑 |
| 4. 多志愿调剂与智能排班模块   | 1. 自动调剂规则：报名截止后，第一志愿未中签的用户，按报名时间优先级，自动匹配第二/第三志愿的剩余名额，保证公平性；<br>2. 自动排班功能：基于用户填报的志愿、可用时段、人数限制，自动生成均衡的排班表，支持按人数、时段自动优化；<br>3. 手动调整功能：组织者可手动调整排班结果、调剂人员、补充名额，调整后自动同步通知；<br>4. 结果公示：排班结果一键生成，支持参与者查看个人结果 | 调剂与排班核心逻辑采用**贪心算法**，按「报名时间优先、志愿优先级优先」的规则实现，逻辑清晰、开发难度低，完全适配校园场景；无需复杂的调度算法，可覆盖90%以上的校园排班需求 |
| 5. 数据导出与通知模块         | 1. 数据导出：支持报名名单、排班表、调剂结果一键导出为Excel格式，适配组织者后续统计需求；<br>2. 本地通知：报名成功、中签结果、活动提醒、调剂状态变更的弹窗通知；<br>3. 数据统计：组织者可查看活动报名率、时段饱和度、调剂成功率等核心数据可视化统计 | 用Python `pandas`/Qt Xlsx模块实现Excel导出，开发成本极低；可视化用Matplotlib/Qt Charts实现，可无缝嵌入Qt界面；通知采用本地弹窗实现，无需短信/邮件接口依赖 |

---

### 三、技术选型（完全符合课程要求）
| 技术环节 | 选型方案                                                     | 选型说明                                                     |
| -------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| 核心开发 | Python + PySide6（Qt官方Python绑定）/ C++ Qt                 | 完全兼容课程强制的Qt前端技术栈，原生桌面端实现，无任何网页界面依赖 |
| 数据存储 | SQLite                                                       | Python/C++原生支持的轻量本地数据库，无需额外安装配置，适配单机使用场景 |
| UI界面   | Qt Widgets                                                   | 基于Qt原生组件实现，贴合课程教学内容，开发效率高，无额外学习成本 |
| 数据处理 | Pandas（Python）/ Qt Xlsx                                    | 快速实现Excel导出与数据统计，学习成本低                      |
| 核心优势 | 全栈本地实现，无服务端/云服务/第三方接口依赖，功能边界清晰，开发成本可控，完整体现软件设计核心思想 |                                                              |

---

### 四、开发周期与风险规避
#### 开发里程碑
1.  第1周：完成核心UI框架、三级权限管理模块、活动配置模块开发；
2.  第2周：完成先到先得报名、小组管理模块开发，打通核心报名闭环；
3.  第3周：完成多志愿调剂、智能排班模块开发，实现核心算法逻辑；
4.  第4周：完成数据导出、通知、统计功能开发，全流程测试与优化。

#### 风险与规避方案
1.  风险：先到先得出现超报、名额冲突 → 规避：基于SQLite事务机制实现名额操作的原子性，加锁保证同一时段名额仅能被一人锁定，彻底解决超报问题；
2.  风险：调剂与排班算法复杂度过高 → 规避：采用简化的贪心算法，优先保证「报名时间+志愿优先级」核心规则，不做过度复杂的智能调度，适配校园场景即可；
3.  风险：权限管理混乱 → 规避：采用标准化RBAC模型，预定义三级角色的固定权限边界，不做动态权限配置，降低开发复杂度。

---

### 五、预期成果与价值
1.  实用价值：解决校园活动报名、排班的高频痛点，可直接用于班级、社团、学院的活动管理，替代手动Excel统计，具备极强的实际使用价值；
2.  课程价值：完整体现MVC分层架构、面向对象设计思想，落地工厂模式、状态模式、观察者模式等经典设计模式，同时包含基础算法实现，完全符合课程评分核心标准；
3.  落地性：功能边界清晰，无过度设计，无第三方依赖，可在课程周期内100%落地，无技术风险，且撞题率极低，是极具竞争力的高分选题。

---

### 六、问题
1.  作为服务类平台，如果仅是单机运行，影响实际应用价值。而如果使用局域网网络通信，那需要使用HTTP相关的技术栈，与课程强制要求的桌面应用相背离，且web开发难度略高。（Qt+requests客户端，Django或FastAPI服务端后端似乎可行）